pkgname="grub-theme-axos"
pkgver="1.0"
pkgrel="1"
pkgdesc="Grub theme for AxOS"
arch=("x86_64")


package(){
    mkdir -p "${pkgdir}/usr/share/grub/themes/axos"
    cp -r ${srcdir}/* ${pkgdir}/usr/share/grub/themes/axos
}
